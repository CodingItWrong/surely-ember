<CategoryList
  @categories={{this.sortedCategories}}
  @onClickHeaderIcon={{this.goToList}}
  @onChooseCategory={{this.handleChooseCategory}}
  @onRefresh={{this.handleRefresh}}
  @onAdd={{this.goToNew}}
/>
{{outlet}}
