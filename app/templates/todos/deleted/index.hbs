<TodoList
  @title="Deleted"
  @icon="delete"
  @todos={{this.sortedTodos}}
  @onChooseTodo={{this.handleChooseTodo}}
  @onRefresh={{action send 'refreshRoute'}}
  as |todo|
>
  Deleted {{relative-datetime todo.deletedAt}}
</TodoList>
