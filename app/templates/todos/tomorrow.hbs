<NewTodoForm
  @placeholder="Add Tomorrow Todo"
  @deferredUntil={{tomorrow}}
  @onAdd={{this.goToList}}
/>

<div class="list-detail">
  <TodoList
    @title="Tomorrow"
    @icon="bedtime"
    @todos={{this.sortedTodos}}
    @onClickHeaderIcon={{this.goToList}}
    @onChooseTodo={{this.handleChooseTodo}}
    @onRefresh={{this.handleRefresh}}
    as |todo|
  />
  {{outlet}}
</div>
