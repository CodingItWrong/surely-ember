<TodoList
  @title="Future"
  @icon="calendar_today"
  @todos={{this.sortedTodos}}
  @onChooseTodo={{this.handleChooseTodo}}
  @onRefresh={{action send 'refresh'}}
  as |todo|
>
  {{capitalize (relative-date todo.deferredUntil)}}
</TodoList>
