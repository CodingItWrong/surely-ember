<PaperForm
  @onSubmit={{this.handleSearch}}
  class="layout-margin"
  data-test-search-form
  as |form|
>
  <form.input
    @placeholder="Search"
    @value={{this.searchText}}
    @onChange={{action (mut this.searchText)}}
    @block={{true}}
    data-test-search-field
  />
</PaperForm>
